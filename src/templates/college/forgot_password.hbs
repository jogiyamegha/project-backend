<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appreciation and Regards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f7f7f7;
        }
        .container {
            width: 80%;
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
            text-align: center;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }
        .content {
            padding: 20px;
        }
        .signature {
            font-weight: bold;
        }
        .font_italic{
            font-style: italic !important;
        }
        .name_header{
            font-style: italic !important;
            font-size:24px;
        }
        .second-line {
            margin-top: 20px;
        }
        .keyword {
    text-decoration: underline;
    /* You can also customize the underline style, color, etc. */
}
    .teamName{
        margin-top: -12px;
    }

    .button-container {
  display: flex;
  justify-content: center;
        /* margin: 0 auto; */
}

.reset-password-button {
  width: 336px;
  height: 60px;
  /* padding: 22px 107px 21px; */
  margin: 30px 152px 30px 100px;
  background-color: #82017F;
  border: none;
  color: white;
  font-size: 14px;
  font-weight: bold;
  text-transform: uppercase;
  cursor: pointer;
}

/* Optional: Add hover effect */
.reset-password-button:hover {
  background-color: #82017F;
}


.Divider {
  height: 2px;
  margin: 29.5px 0 29px 1px;
  padding: 1px 0;
  background-color: #f1f1f1;
}


/* Optional: Add hover effect */
.reset-password-button:hover {
  background-color: #82017F;
}
.Line-1 {
  /* width: 600px; */
  height: 2px;
  margin: 30px 0 0px 0px;
  background-color: #f1f1f1;
}
.finishingLine1{
    margin-top: 29px;
    font-size: 12px;
}
.finishingLine2{
    margin-top: -7px;
    margin-left: -5px;
    font-size: 12px;
}

    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <div class="Line-1"></div>
            <p>
                <div class="name_header signature">
                    Hi {{name}},
                </div>
            </p>
                    
            <p class="font_italic">We received a request to reset your password. Use the button below to setup a new password for your account.</p>
            <p class="second-line font_italic">Or you can set new password using this Code: <span class="signature keyword">{{code}}</span></p>
            <div class="button-container">
                <button class="reset-password-button">Reset Password</button>
              </div>
            <p class="signature">Thank You</p>
            <p class="teamName">Team Onward</p>
            <div class="Divider"></div>
            <p  class="finishingLine1 font_italic" >If you didn't request to change your Onward password.</p>
            <p class="finishingLine2 font_italic">You don't have to do anything. So that's easy.</p>
        </div>
    </div>
</body>
</html>